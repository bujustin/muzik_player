//Empty get function that will change the cover image
function changeCoverImage(){
    var image = document.getElementById("cover_image");
    //image.src = string; stirng will be a url that changes the image
}

//Empty get function that will change the artists info
function changeCoverArtist(){
    var text = document.getElementById("song_artist");
    // text.innerHTML='['+string+']'; string will be the artist of current song
}
